```python
bag_list = self.get_obj('bag')
tgt_idx = self.find_instance_in_category(instance = 'bag next to the box', category = 'bag')
tgt_bag_list = []
for idx in tgt_idx:
    tgt_bag_list.append(bag_list[idx]) 
output_var = tgt_bag_list
```