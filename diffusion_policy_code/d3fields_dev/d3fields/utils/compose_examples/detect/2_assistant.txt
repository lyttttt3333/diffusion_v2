```python
bag_list = self.get_obj('bag')
tgt_idx = self.find_instance_in_category(instance = 'bags next to the box', category = 'bag')
tgt_bag = []
# don't use list comprehension
for i in tgt_idx:
    tgt_bag.append(bag_list[i])
output_var = tgt_bag
```