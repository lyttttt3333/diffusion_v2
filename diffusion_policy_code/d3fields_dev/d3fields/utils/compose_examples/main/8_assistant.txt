```python

# Determine target object, the ball outside the box
ball = self.detect("ball outside the box")[0]


# Determine target object, the box on the first row and second column
box = self.detect("on the first row and second column")[0]


output_var = {}
output_var["ball"] = ball
output_var["box"] = box

```

