```python

query_list = ["cube block", "sphere block"]
output_var = []
for query_block_name in query_list:
    attn_dict = {}

    query_block_pts = self.detect(query_block_name)[0]
    attn_dict["block"] = query_block_pts

    query_block_pts = self.detect(f"corresponding box for {query_block_name} with the same shape blocks in it")[0]
    attn_dict["box"] = box_list[idx]

    output_var.append(attn_dict)

```

