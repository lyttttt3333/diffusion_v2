```python

block_list = self.detect("sphere_blocks")
box_list = self.detect("boxs")

output_var = []
for idx, block_pts in enumerate(block_list):
    attn_dict = {}
    attn_dict["block"] = block_pts
    attn_dict["box"] = box_list[idx]

    output_var.append(attn_dict)

```





